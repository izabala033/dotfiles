# Portable default for single-monitor sessions.
# The dual-screen HDMI-A-2/DP-1 layout is applied at runtime only when both
# outputs are present, and keeps scale at 1 there.
# Increase the final value to make the laptop screen bigger, or lower it for more space.
monitor=,preferred,auto,1.25
