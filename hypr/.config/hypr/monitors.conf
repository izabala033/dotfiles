# Portable default for any connected monitor.
# The dual-screen HDMI-A-2/DP-1 layout is applied at runtime only when both
# outputs are present, so this repo can also boot cleanly on one-screen laptops.
monitor=,preferred,auto,1
